#pragma once

// typedefs
using int32 = int32_t;
using uint32 = uint32_t;

// engine global vars
const extern int32	VOXEL_IN_CHUNK_XY;	// voxel count along with x or y axis
const extern int32	VOXEL_IN_CHUNK_Z;	// voxel count in z(vertical) axis
const extern float	VOXEL_SIZE;
const extern float	CHUNK_SIZE_XY;
const extern float	CHUNK_SIZE_Z;
const extern float	CHUNK_LOADING_RADIUS;

#define XXXX 10